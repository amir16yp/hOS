# hos-init: which services the supervisor starts.
#
# Every service is started by default and restarted if it exits. To turn one
# off, or to run a different program, name it here:
#
# [service hos-ntpd]
# enabled = no
#
# [service hos-netd]
# program = /bin/hos-netd
# arguments =
